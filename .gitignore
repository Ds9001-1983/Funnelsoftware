node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables
.env
.env.local
.env.production

# Lighthouse-Reports (lokal generiert)
lighthouse-report.html

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Test-Coverage (lokal generiert)
coverage/

# Hochgeladene Dateien zur Laufzeit — gehören nie ins Repo.
# private-uploads/ enthält Screenshots aus Fehlermeldungen und kann
# personenbezogene Daten zeigen (siehe server/routes.ts, Bug-Melder).
uploads/*
!uploads/.gitkeep
private-uploads/

# macOS/iCloud-Sync-Duplikate ("Name 2.ext") nie committen
**/* 2.png
**/* 2.webp
**/* 2.mp4
**/* 2.webm
